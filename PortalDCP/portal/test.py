#from portal import database,app
#from portal.models import Atendimento

#with app.app_context():
    #p = Atendimento.query.filter_by(id=4).first()
    #p.nome = 'Couves José'
    #database.session.add(p)
    #database.session.commit()
    #database.drop_all()
    #database.create_all()
